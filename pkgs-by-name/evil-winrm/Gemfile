source 'https://rubygems.org'

# Minimums follow evil-winrm's own gemspec (>= 2.3.7 / >= 1.3.2).
gem 'winrm', '>= 2.3.7'
gem 'winrm-fs', '>= 1.3.2'
# Explicitly listed: no longer default gems in Ruby 3.4 (csv) or needed
# directly by evil-winrm.rb. readline/readline-ext/syslog/benchmark stay
# out: ruby already ships them, and readline-ext cannot compile here
# (no readline headers in the gem build env).
gem 'csv'
gem 'stringio'
gem 'logger'
gem 'fileutils'
